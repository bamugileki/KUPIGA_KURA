<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ $election->title_en }} - Results</title>
    <style>
        @page { margin: 20mm; size: A4 portrait; }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Arial, sans-serif; font-size: 12pt; color: #1a1a1a; line-height: 1.4; }
        .header { text-align: center; border-bottom: 3px solid #1e3a8a; padding-bottom: 15px; margin-bottom: 20px; }
        .header h1 { font-size: 18pt; color: #1e3a8a; margin-bottom: 4px; }
        .header h2 { font-size: 14pt; color: #333; font-weight: normal; }
        .header .meta { font-size: 10pt; color: #666; margin-top: 8px; }
        table { width: 100%; border-collapse: collapse; margin: 15px 0; }
        th { background: #1e3a8a; color: white; padding: 8px 10px; text-align: left; font-size: 10pt; }
        td { padding: 6px 10px; border-bottom: 1px solid #ddd; font-size: 10pt; }
        tr:nth-child(even) { background: #f8fafc; }
        .winner { background: #fef3c7 !important; font-weight: bold; }
        .total-row { border-top: 2px solid #1e3a8a; font-weight: bold; }
        .footer { text-align: center; font-size: 9pt; color: #999; margin-top: 30px; border-top: 1px solid #ddd; padding-top: 10px; }
        .badge { display: inline-block; padding: 2px 8px; border-radius: 3px; font-size: 9pt; }
        .badge-winner { background: #fef3c7; color: #92400e; }
        .badge-closed { background: #e5e7eb; color: #374151; }
    </style>
</head>
<body>
    <div class="header">
        <div style="display: flex; align-items: center; justify-content: center; gap: 15px; margin-bottom: 10px;">
            <img src="{{ asset('images/TUME.png') }}" alt="TUME Logo" style="height: 60px; width: 60px; object-fit: contain;">
            <div>
                <h1>Tume Huru ya Taifa ya Uchaguzi Tanzania</h1>
                <h2>Election Results Report</h2>
            </div>
        </div>
        <div class="meta">
            <strong>{{ $election->title_en }}</strong>
            @if($position) &mdash; {{ session('lang') == 'sw' ? $position->name_sw : $position->name_en }} @endif
            <br>
            Voting Period: {{ $election->start_time->format('d M Y H:i') }} &ndash; {{ $election->end_time->format('d M Y H:i') }}
            <br>
            Generated: {{ now()->format('d M Y H:i:s') }}
            | Status: <span class="badge {{ $election->status === 'closed' ? 'badge-closed' : '' }}">{{ ucfirst($election->status) }}</span>
            @if($election->winner_declared)
                | <span class="badge badge-winner">&#9733; Winner Declared</span>
            @endif
        </div>
    </div>

    <table>
        <thead>
            <tr>
                <th>Rank</th>
                <th>Candidate</th>
                <th>Party</th>
                @if($election->election_type === 'presidential')
                <th>Running Mate</th>
                @endif
                <th>Constituency</th>
                <th>Votes</th>
                <th>Percentage</th>
            </tr>
        </thead>
        <tbody>
            @foreach($results['candidates'] as $item)
            <tr class="{{ $election->winner_declared && $loop->first ? 'winner' : '' }}">
                <td>{{ $item['rank'] }}{{ $election->winner_declared && $loop->first ? ' &#9733;' : '' }}</td>
                <td>{{ $item['candidate']->full_name ?? $item['candidate']->user->full_name }}</td>
                <td>{{ $item['candidate']->party_abbreviation ?? '' }}</td>
                @if($election->election_type === 'presidential')
                <td>{{ $item['candidate']->running_mate_name ?? '-' }}</td>
                @endif
                <td>{{ $item['candidate']->constituency ?? '-' }}</td>
                <td>{{ $item['vote_count'] }}</td>
                <td>{{ $item['percentage'] }}%</td>
            </tr>
            @endforeach
            <tr class="total-row">
                <td colspan="{{ $election->election_type === 'presidential' ? 5 : 4 }}">Total Votes Cast</td>
                <td>{{ $results['total_votes'] }}</td>
                <td>100%</td>
            </tr>
        </tbody>
    </table>

    @if($election->winner_declared && !empty($results['candidates']))
    <div style="margin-top: 20px; padding: 10px 15px; background: #fef3c7; border-left: 4px solid #d97706; border-radius: 4px;">
        <strong style="color: #92400e;">&#9733; WINNER:</strong>
        <span style="color: #92400e;">
            {{ $results['candidates'][0]['candidate']->full_name ?? $results['candidates'][0]['candidate']->user->full_name }}
            ({{ $results['candidates'][0]['vote_count'] }} votes, {{ $results['candidates'][0]['percentage'] }}%)
        </span>
    </div>
    @endif

    <div class="footer">
        <p>This is an official results report generated by the Tume Huru ya Taifa ya Uchaguzi system.</p>
        <p>Page 1 of 1 &mdash; Generated {{ now()->format('d M Y H:i:s') }}</p>
    </div>
</body>
</html>